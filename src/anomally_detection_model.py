import random

class AnomalyDetector():
    # This class has the role of identifying anomalies  within a provided document.
    # According to the requirements, the algorithm is simply a result of a random number.
    #  Currently, it functions as a basic framework or template.

    @classmethod
    def calc_anomaly(cls):
        return random.randint(0,4)
